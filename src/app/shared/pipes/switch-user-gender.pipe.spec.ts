import { SwitchUserGenderPipe } from './switch-user-gender.pipe';

describe('SwitchUserGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchUserGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
