import { SwitchCardStatusPipe } from './switch-card-status.pipe';

describe('SwitchCardStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchCardStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
