import { SwitchOutletTypePipe } from './switch-outlet-type.pipe';

describe('SwitchOutletTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchOutletTypePipe();
    expect(pipe).toBeTruthy();
  });
});
