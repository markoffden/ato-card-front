import { SwitchUserActivatedPipe } from './switch-user-activated.pipe';

describe('SwitchUserActivatedPipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchUserActivatedPipe();
    expect(pipe).toBeTruthy();
  });
});
