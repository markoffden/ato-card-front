import { SwitchUserRolePipe } from './switch-user-role.pipe';

describe('SwitchUserRolePipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchUserRolePipe();
    expect(pipe).toBeTruthy();
  });
});
